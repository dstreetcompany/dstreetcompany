body {
  margin: 0;
  padding: 0;
  font-family: 'Bebas Neue', cursive;
  background: url('background.jpg') no-repeat center center fixed;
  background-size: cover;
  color: white;
  text-align: center;
}

.container {
  padding: 20px;
  max-width: 800px;
  margin: 0 auto;
}

.logo {
  width: 120px;
  margin-bottom: 10px;
}

.vegas-title {
  font-size: 64px;
  color: gold;
  text-shadow: 0 0 15px red, 0 0 25px orange;
  margin-bottom: 10px;
}

.phrase {
  font-size: 28px;
  margin: 15px 0;
}

.rules {
  font-size: 20px;
  color: #fff;
  background: rgba(0, 0, 0, 0.5);
  padding: 10px;
  border-radius: 10px;
  margin-bottom: 20px;
}

.wheel-container {
  position: relative;
  display: inline-block;
}

.pointer {
  position: absolute;
  top: -20px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 20px solid transparent;
  border-right: 20px solid transparent;
  border-bottom: 40px solid yellow;
  z-index: 10;
}

#spin {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 120px;
  height: 120px;
  border-radius: 50%;
  font-size: 22px;
  font-weight: bold;
  background: radial-gradient(circle, gold 40%, orange);
  color: black;
  border: 4px solid white;
  box-shadow: 0 0 25px gold;
  cursor: pointer;
  z-index: 9;
}

#result {
  font-size: 36px;
  margin-top: 30px;
  font-weight: bold;
  text-shadow: 0 0 5px black;
}

.note {
  font-size: 14px;
  margin-top: 10px;
  opacity: 0.7;
}

